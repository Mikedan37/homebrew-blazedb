# homebrew-blazedb

Homebrew tap for the BlazeDB CLI.

## Install

```bash
brew tap Mikedan37/blazedb
brew install blazerepl
```

## Commands

- `blazedb` (canonical command)
- `blazerepl` (alias to `blazedb`)

Example:

```bash
blazerepl start
```
